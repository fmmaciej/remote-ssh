[user]
    name = Your Name
    email = your.email@example.com

# In remote-ssh shells this file is also applied as a session override.
# It can win over repository .git/config without writing to that repository.

[core]
    editor = vim
